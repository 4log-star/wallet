import { Box } from "@/components/ui/Box"
import { StyleSheet } from "react-native"



export const HomeScreen = () => {
    return (
        <Box style={style?.box} background={"background"}>
        </Box>
    )
}

const style = StyleSheet.create({
    box : {
        width : "100%",
        height : "100%"
    }
})

